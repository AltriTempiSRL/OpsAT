# ── Secretos y credenciales ──────────────────────────────────────────────────
.env.txt
.env.local
.jwt-secret
*.key
*.pem

# ── Datos en tiempo de ejecución (PII / datos del negocio) ───────────────────
# Globs para cubrir TODAS las colecciones (incluidas las nuevas) sin listarlas
# una por una. Ninguna de estas familias contiene archivos que deban trackearse.
wwp-*.json              # tasks, users-auth, sessions, roles, locations(GPS), inventario, etc.
sdv-*.json              # solicitudes de despacho/devolución, alertas, auditorías
emp-*.json              # materiales/reglas de empaque
ops-notifications.json
push-subscriptions.json
vapid-keys.json         # ⚠ contiene la clave VAPID privada
reposiciones.json
despachos-obsoleto.json
despacho-obsoleto-seq.json
vehiculos-inspecciones.json
averias.json
contenedores.csv
.media-r2-migrated      # marcador de migración a R2

# ── Fotos/videos subidos por usuarios (evidencia) ────────────────────────────
av-fotos/
wwp-fotos/
empaque-fotos/
emp-fotos/
desp-fotos/
sdv-adjuntos/
prod-img/
inspection/
inspections/
inspeccion/

# ── Archivos temporales ───────────────────────────────────────────────────────
*.tmp
*.log
*.backup-*.html
temp_*.html

# ── Dependencias ──────────────────────────────────────────────────────────────
node_modules/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── Backups de datos en tiempo de ejecución ──────────────────────────────────
backups/
backup-wwp-*.json
# Respaldos que genera saveJson atómico (write .tmp → rename; .bak = estado previo)
*.bak
*.tmp

# ── Herramientas de desarrollo ────────────────────────────────────────────────
.claude/
.netlify/
.railway-ssh/

# ── Entorno local de desarrollo ───────────────────────────────────────────────
.env
data-local/
stress-test.js

# Runtime/scratch local
wwp-audit.json
proxy.js.tmp.*
.compare-render/

# ── Cerebro de agentes: vive en C:\Users\Gabriel Ramirez\Agentes-Estandar (fuera del proyecto) ──
agentes-estandar/

# Recuperacion incidente 25-jun (datos sensibles: hashes, audit log) — NO commitear
_RECOVERY_2026-06-25/
_bltest/
